// Place all the styles related to the monsters controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/


.make_monster {
  background-image: url(asset_path('cave_fisher.png'));
  background-position: 330% 25%;
}

.new_monster {
  margin: 100px 0 0 200px;
}

.show_monsters img {
  max-width: 500px;
  position: absolute;
  margin: 300px;
  opacity: .2;
}

.monster_index .content {
  margin: 10px 0 0 30px;
}

.monster_index .button_to {
  text-align: right;
  margin-right: 20px;
}

.monster_index img {
  max-height: 220px;
}

.monster_index .info {
  display: inline-block;
  margin: 10px;
}

.item {
  height: 450px;
}

.pagination {
    background: #E8E8E8;
    padding: 20px;
    margin-bottom: 20px;
}

.monster_index .page {
    display: inline-block;
    padding: 0px 9px;
    margin-right: 4px;
    border-radius: 3px;
    border: solid 1px #c0c0c0;
    background: #e9e9e9;
    box-shadow: 0px 1px 0px rgba(255,255,255, .8);
    text-decoration: none;
    color: #717171;
    text-shadow: 0px 1px 0px rgba(255,255,255, 1);
}

.monster_index .page:hover {
    background: #fefefe;
}

.monster_index  .page.active {
    border: none;
    background: #616161;
    box-shadow: inset 0px 0px 8px rgba(0,0,0, .5), 0px 1px 0px rgba(255,255,255, .8);
    color: #f0f0f0;
    text-shadow: 0px 0px 3px rgba(0,0,0, .5);
  }

